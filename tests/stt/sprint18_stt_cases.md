# Sprint 18 STT Cases

- Default provider path uses configured provider in `STT_PROVIDER_DEFAULT`
- Provider errors map to stable error codes
- Voice answer persistence stores `source='voice'` and `sttMeta`
- Text answer path remains unchanged
