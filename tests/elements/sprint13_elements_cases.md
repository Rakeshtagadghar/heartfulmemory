# Sprint 13 Elements v1 Test Cases

- Unit: `insertGrid` creates deterministic grid metadata (`childrenIds`, `cells`).
- Unit: `fillFrameWithImage` merges imageRef without destroying existing frame placeholder metadata.
- Integration: Elements panel quick chips and category items call `onInsertElement(...)`.
- Manual: drag upload/photo card onto frame placeholder fills selected target frame.

