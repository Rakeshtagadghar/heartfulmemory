import { test } from "vitest";

test.skip("RLS owner/anon integration requires configured Supabase env + seeded users", () => {
  // Sprint 4 harness placeholder. Execute against a real Supabase project after applying migrations/policies.
});
